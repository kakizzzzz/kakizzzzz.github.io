# Kaki's Design Console

An immersive portfolio site built with Vite and React.

## Development

**Prerequisites:** Node.js

1. Install dependencies:
   `npm install`
2. Start the dev server:
   `npm run dev`

## Production

1. Build the site:
   `npm run build`
2. Preview the production build:
   `npm run preview`
